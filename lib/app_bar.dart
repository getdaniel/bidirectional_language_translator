import 'package:flutter/material.dart';

AppBar appBarState() {
  return AppBar(
    title: const Text(
      "Bidirectional Language Translator",
      style: TextStyle(
        fontWeight: FontWeight.bold,
      ),
    ),
  );
}
